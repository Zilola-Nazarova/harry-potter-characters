import styles from '@/styles/Header.module.css'

const Header = () => {
  return (
    <header className={styles.header}>
      <h1>This is Header!</h1>
      <p>Here are useful links and logo</p>
    </header>
  );
};

export default Header;
